deployAs:
  - name: trustelev8service
    applicationType: service
    endpoint: /trustelev8
    configurationType:
      CTP_PROJECT_KEY: secret
      CTP_CLIENT_ID: secret
      CTP_CLIENT_SECRET: secret
      CTP_SCOPE: secret
      CTP_REGION : standard
